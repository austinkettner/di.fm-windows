<common:LayoutAwarePage
    x:Name="pageRoot"
    x:Class="DI.FM.View.ChannelPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DI.FM.View"
    xmlns:common="using:DI.FM.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <common:DurationConverter x:Key="DurationConverter" />
    </Page.Resources>
    
    <Page.BottomAppBar>
        <AppBar BorderBrush="{x:Null}">
            <AppBar.Background>
                <SolidColorBrush Color="{Binding NowPlayingItem.Color2}" Opacity="0.7" />
            </AppBar.Background>
        </AppBar>
    </Page.BottomAppBar>

    <Grid Background="{Binding NowPlayingItem.Color1}">
        <Grid.RowDefinitions>
            <RowDefinition Height="110"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#212121" BorderThickness="0,0,0,5" BorderBrush="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="109" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Margin="42,0,0,30" IsEnabled="{Binding Frame.CanGoBack, ElementName=pageRoot}" Style="{StaticResource BackButtonStyle}" Click="GoBack" />
                <TextBlock Grid.Column="1" Margin="0,18,0,0">
                    <Run Text="{Binding NowPlayingItem.Name}" Foreground="White" FontSize="40.2" FontWeight="SemiLight" />
                    <LineBreak />
                    <Run Text="{Binding NowPlayingItem.Description}" Foreground="#c3c3c3" FontSize="17.4" FontWeight="SemiLight" />
                </TextBlock>
                <Image Grid.ColumnSpan="2" Source="../Assets/TopArt.png" Stretch="None" VerticalAlignment="Bottom" HorizontalAlignment="Right" />
            </Grid>
        </Border>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            
            <Grid Grid.ColumnSpan="2">
                <Grid HorizontalAlignment="Left">
                    <Image Source="{Binding NowPlayingItem.Image}" />
                    <Rectangle Width="300" HorizontalAlignment="Right">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                <GradientStopCollection>
                                    <GradientStop Color="#00000000" Offset="0" />
                                    <GradientStop Color="{Binding NowPlayingItem.Color1}" Offset="1" />
                                </GradientStopCollection>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                </Grid>
            </Grid>
            
            <Grid Grid.Column="1">
                <ScrollViewer>
                    <StackPanel Margin="40,35,40,30">
                        <TextBlock Margin="0,0,0,8" Text="NOW PLAYING" FontSize="18.6" FontWeight="Bold" />
                        <Grid Height="170">
                            <Grid.Background>
                                <SolidColorBrush Color="{Binding NowPlayingItem.Color2}" Opacity="0.7" />
                            </Grid.Background>
                            <Border BorderThickness="1" BorderBrush="#48000000" />
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Margin="10" Text="{Binding NowPlayingItem.NowPlaying.Track}" FontSize="29.3" FontWeight="Light" TextWrapping="Wrap" TextTrimming="WordEllipsis" />
                                <TextBlock Grid.Row="1" Margin="10" FontSize="21.3" FontWeight="Normal" VerticalAlignment="Bottom" HorizontalAlignment="Left">
                                    <Run Text="{Binding Path=NowPlayingPosition, Converter={StaticResource DurationConverter}}" />
                                    <Run Text="/" />
                                    <Run Text="{Binding Path=NowPlayingItem.NowPlaying.Duration, Converter={StaticResource DurationConverter}}" />
                                </TextBlock>
                                <Button Grid.RowSpan="2" Margin="10" VerticalAlignment="Bottom" HorizontalAlignment="Right" Style="{StaticResource PlayIconButtonStyle}" Click="ButtonPlay_Click" />
                            </Grid>
                        </Grid>
                        <Grid Margin="0,15,0,0">
                            <Button HorizontalAlignment="Left" Click="ButtonPrev_Click">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="1" TextAlignment="Left">
                                        <Run Text="PREV" FontSize="16.2" FontWeight="Bold" />
                                        <LineBreak />
                                        <Run Text="{Binding NowPlayingItem.Prev.Name}" FontSize="16.8" FontWeight="Normal" />
                                    </TextBlock>
                                </Grid>
                            </Button>
                            <Button HorizontalAlignment="Right" Click="ButtonNext_Click">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock TextAlignment="Right">
                                        <Run Text="NEXT" FontSize="16.2" FontWeight="Bold" />
                                        <LineBreak />
                                        <Run Text="{Binding NowPlayingItem.Next.Name}" FontSize="16.8" FontWeight="Normal" />
                                    </TextBlock>
                                </Grid>
                            </Button>
                        </Grid>

                        <TextBlock Margin="0,40,0,8" Text="TRACK HISTORY" FontSize="18.6" FontWeight="Bold" />
                        <ListView ItemsSource="{Binding NowPlayingItem.TrackHistory}">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Margin" Value="0,1" />
                                    <Setter Property="Height" Value="48" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <Border>
                                                    <Border.Background>
                                                        <SolidColorBrush Color="Black" Opacity="0.2" />
                                                    </Border.Background>
                                                    <Grid Margin="10,0" VerticalAlignment="Center">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock FontSize="14.63" FontWeight="SemiLight">
                                                            <Run Text="{Binding Index}" />
                                                            <Run Text=". " />
                                                            <Run Text="{Binding Track}" />
                                                        </TextBlock>
                                                        <TextBlock Grid.Column="1" Text="{Binding Path=Duration, Converter={StaticResource DurationConverter}}" FontSize="14.63" FontWeight="SemiLight" />
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</common:LayoutAwarePage>
